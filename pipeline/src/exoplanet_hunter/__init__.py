"""Exoplanet Hunter V2 — self-refreshing, self-validating transit-detection platform.

Core science modules (preprocess, models, features, search, eval) are salvaged
from V1; the training loop, data pipeline, validation gates, and serving layer
are V2 rewrites. See the repository README for the branch-by-branch build order.
"""

__version__ = "2.0.0.dev0"
