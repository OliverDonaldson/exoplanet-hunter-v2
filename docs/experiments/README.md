# The record

What was measured, in the order it happened, one file per stage or audit. Every
file was moved verbatim from `docs/roadmap.md` on 2026-09-04 and is frozen: a
correction is a dated note appended under the entry it corrects, never an edit.
Pre-registrations sit immediately before the result they fixed the reading of.
The old-to-new stage numbering is at [`../roadmap.md`](../roadmap.md) §1c, and
every old section number is a pointer there.

Two conventions run through all of it. A margin is read against the noise floor
measured in the same run — by 2·√(sd_c²/n_c + sd_i²/n_i), the se of a difference,
corrected 2026-09-17 from the 2·sd/√n this line carried before — and a floor
belongs to the architecture it was measured on. A result landing outside the
terms fixed before it ran is recorded as falsified, never re-specified. The
outcome column below is each entry's own reading of itself, never a later
reinterpretation. Each row was drafted from the file, then checked against it by
two independent adversarial readers, one on the numbers and one on the claims;
the fourteen rows they flagged were corrected by hand against the file before
this index was published.

| file | covers | dates | outcome | one line |
|---|---|---|---|---|
| [`stage-01-03-inputs-and-rebaseline.md`](stage-01-03-inputs-and-rebaseline.md) | stages 1–3 · §3.1 | 2026-08-05 to 2026-08-08 | mixed | Stages 1–3 done: an eleven-branch 5,423-example view set and a 3.6 GB DV archive, shards 2.6x larger at 301/31; the momentum-dump branch and 33x33 stamps could not be built as specified, and the re-baselined per-mission summary is what lets the gate decide. |
| [`audit-2026-08-07.md`](audit-2026-08-07.md) | audit of the recorded numbers · §2b–2e | 2026-08-07 to 2026-08-08 | audit | Every run-1 number reproduces, with one misprint corrected and one false claim about the TESS low-count cells retracted; run 2 lost all five paired folds (mean −0.0313, pairing flagged inexact) and every re-baselined mission; the TESS gate had never engaged. |
| [`stage-04-branch-runs.md`](stage-04-branch-runs.md) | stage 4 · §3.2 | 2026-08-05 to 2026-08-08 | rejected | Every branch arm rejected on TESS recall @1% FPR (0.238, 0.126, 0.145, 0.236 vs 0.307); resolution hypothesis falsified (Kepler +0.0707) and capacity closed (paired -0.0035, d=-0.44). |
| [`stage-05-viewset.md`](stage-05-viewset.md) | stage 5 · §3.3 | 2026-08-08 | confirmed | Candidate view set rebuilt cold as budgeted — 5,346 rows at 2001/201, 309 MB, 95 minutes — well-formed and scoreable by run 3's fold-0 checkpoint; the entry also re-specifies stage 7's criterion and plans stage 6. |
| [`stage-06-recall-floor.md`](stage-06-recall-floor.md) | stage 6 · §3.4 | 2026-08-08 to 2026-08-09 | confirmed | The recall noise floor is measured: pooled seed sd 0.0292 gives a ~0.034 decision floor, run 3's rejection is sound at 4.8x it, and the capacity arm's gain was real but stays unactionable at 0.220 against the champion's 0.307. |
| [`execution-order-2026-08-09.md`](execution-order-2026-08-09.md) | execution order and impact ranking · §2f–2g | 2026-08-08 to 2026-08-09 | decision | Execution order fixed as 7i→8→9→10→7ii→11→12 by splitting stage 7 into instrument and reading, removing all three backward edges; integers kept as names; stage 8 ranked highest impact, stage 7 lowest. |
| [`readiness-contract-2026-08-09.md`](readiness-contract-2026-08-09.md) | the readiness contract · §2h | 2026-08-09 | decision | Readiness after stage 11 judged yes with two exceptions: an unexplained +0.1446 Kepler cell and no published catalogue; item 1 likely resolves by closing the branch line, not promotion. |
| [`stage-07-attribution-sweep.md`](stage-07-attribution-sweep.md) | stage 7, the attribution sweep · §3.5 | 2026-08-09 | mixed | Stage 7's 26.4% host-pass criterion is unmeasurable as written and was re-specified; the three-family leave-one-out sweep read null on every arm, and spending no further stage-7 compute before stage 8 is recorded as a recommendation, not a decision taken. |
| [`stage-07i-control-arm-harness.md`](stage-07i-control-arm-harness.md) | stage 7i · §3.6 | 2026-08-09 to 2026-08-12 | null | Stage 7's criterion is not met: on 580 baseline-matched hosts the branch model has no measured advantage over the incumbent on either criterion, and the one prediction the file calls falsified is falsified at its own F1 cut and not at the other operating point. |
| [`gate-calibration-2026-08-12.md`](gate-calibration-2026-08-12.md) | promotion gate audit · §3.7 | 2026-08-12 | audit | Three gate defects found and fixed, none of which changes a recorded verdict — only what a verdict was allowed to claim; `paired_folds` stays computed but never gating and is recorded as a known limitation rather than fixed. |
| [`observation-baseline.md`](observation-baseline.md) | observation baseline · §3.8 | 2026-08-05 to 2026-08-12 | audit | Score–baseline correlation is a genuine label confound (label +0.387 on TESS); the branch model amplifies it to +0.5155, +0.13 above the labels, so the fix is label-distribution intervention, not architecture. |
| [`stage-08-labels-and-negatives.md`](stage-08-labels-and-negatives.md) | stage 8 · §3.9 | 2026-08-08 to 2026-08-15 | mixed | Propensity weighting removed the branch model's amplification of baseline dependence (gap −0.1336, 3.3× bar) at no recall cost; synthetic null, arm S unreadable, labels untouched, all four predictions falsified, split win qualified. |
| [`ui-scaffold-audit-2026-08-13.md`](ui-scaffold-audit-2026-08-13.md) | UI scaffold audit · §3.10 | 2026-08-13 | audit | Scaffold audit against the API: a fourth tab was added for the branch evidence stage 11 will produce, the probability-vs-epoch panel was identified as unphysical with `per_fold` and `prob_std` named as the better versions already on the contract, and two computed quantities the scaffold never showed were listed. |
| [`stage-10-5-ensemble.md`](stage-10-5-ensemble.md) | stage 10.5 · §3.11 | 2026-08-12 to 2026-08-15 | mixed | Both ensemble arms clear their bars on shortlist recall (0.4362 and 0.4223 against the common-fold dual-view member's 0.3046), so the branch line's value is as a complement; on host-scoring, the defect W2 names, the branch models are better than the incumbent by a margin excluding zero, and stage 8's qualified second win is recorded as not to be banked. |
| [`forward-plan-2026-08-16.md`](forward-plan-2026-08-16.md) | the forward plan as of 2026-08-16 · §4 intro, §4.3–4.8 | 2026-08-14 to 2026-08-20 | plan | The forward plan, not an experiment: the remaining items re-costed at ~45–60 h plus an unestimated UI, with the probable ending recorded as the branch line closed in writing and `ca906040`, or its stage 10 retune, staying served. |
| [`refresh-gate-calibration-4-1.md`](refresh-gate-calibration-4-1.md) | refresh gate calibration · §4.1–4.1d | 2026-08-15 to 2026-08-17 | mixed | Refresh gate calibrated: recall floor measured at 0.0733 (3.7x the 0.02 legacy constant); control lane wired after its first check was falsified and replaced; weekly loop had never been able to promote; nothing promoted. |
| [`stage-09-difference-image.md`](stage-09-difference-image.md) | stage 9 and the ExoMiner++ readout · §4.2, §4.2b | 2026-08-17 to 2026-08-20 | unmeasurable | Difference-image branch built and run; its W2 falsification test (control-arm host-AUC) is unmeasurable because the stage 7i harness zeroes the branch on control rows; predictions 2–4 confirm it costs nothing, none show it delivers anything. |
| [`phases-pre-registration-4-2c.md`](phases-pre-registration-4-2c.md) | the four phases, pre-registered · §4.2c | 2026-08-20 | plan | Pre-registration only, nothing run: fixes phase order (console first, seed-sweep floors before Phase 1 is read or stage 10 starts) and the falsification thresholds each phase must meet; no promotion. |
| [`phase-1-build-4-2d.md`](phase-1-build-4-2d.md) | Phase 1 and Phase 1a build · §4.2d | 2026-08-27 to 2026-08-28 | built-not-run | Target-position channel and momentum-dump view built for Phase 1; neither arm has run. Box centre misses the target's pixel on 77.8% of stamps; stage-9 dv_usable baseline recorded (D−C −0.0075, 0.33x floor). |
| [`standing-audits.md`](standing-audits.md) | standing audits · §5 | 2026-08-07 to 2026-08-15 | audit | Four audits done and acted on: ExoMiner delta 6/10, security clean with rate limiting added and protobuf CVE accepted, nothing to delete, DVC pointer move affects no measured number. |
| [`change-log.md`](change-log.md) | change log · §7 | 2026-08-14 to 2026-08-17 | audit | Record restructure verified mechanically lossless; Champion rename left pre-registrations, recorded results and the DVC path unchanged; docs consolidated to eight, W14 closed; 3.11c's reproduction script untracked, pre-run authorship unverifiable. |
| [`phase-1-reading-criterion.md`](phase-1-reading-criterion.md) | Phase 1 pre-registration · how it would be read | 2026-09-05 | plan | Which of the two candidate floors governs the Phase 1 contrast, fixed while arm D″ was still training, with an explicit UNRESOLVED if they disagreed. They did not. |
| [`phase-1-result.md`](phase-1-result.md) | Phase 1 result · the mechanism test | 2026-09-04 to 2026-09-05 | falsified | The target-position channel changes nothing: TESS recall @1% FPR on dv_usable rows moves −0.0254 at 0.26x its floor, so the reference-frame explanation is falsified, no third stamp variant is commissioned and the branch line closes. |
| [`benchmark-comparability-2026-09-10.md`](benchmark-comparability-2026-09-10.md) | benchmark comparability audit · the five cited studies against ours | 2026-09-10 | falsified | The koi_score inflation hypothesis is falsified in its mechanism — the threshold discards 4 of 1,026 dropped FPs, the other 1,022 leave on DR25 membership — and the counterfactual Kepler AUC is bounded at 0.985–0.988, worth 0.004–0.007 not the 0.081 Kepler–TESS gap; of six candidate comparisons only TESS 0.9100 against ExoMiner++ 0.998 survives, with a caveat, and recall @1% FPR has no published counterpart at all. |
| [`centroid-flag-calibration-2026-09-13.md`](centroid-flag-calibration-2026-09-13.md) | the centroid flag against labels · #22 | 2026-09-13 | measured | The statistic is informative (AUC 0.7339 for predicting a false positive) and the 3.0σ cut is miscalibrated: it fires on 43.5% of the labelled population and 27.6% of confirmed planets. The scale is the real defect — 1.21% of targets exceed 1000σ, max 10,436σ — so the threshold was measured and deliberately left unchanged pending a floor on the denominator. |
| [`p2-stage-1-instrument-2026-09-17.md`](p2-stage-1-instrument-2026-09-17.md) | P2 Stage 1 · the instrument, before another model is trained | 2026-09-17 | measured | Four defects in how the project decides, all corrected against artefacts already on disk, nothing trained. The seed sd was one arm's on 2 df; pooled over 15 multi-member runs it is **0.0108 branch (26 df) and 0.0062 dual-view (4 df)**, and the MDE carried one seed term where a contrast needs two — the corrected bar at 5 members is **0.0201 branch, 0.0126 dual-view**. `decision_floor` divided the borrowed champion variance by the *candidate's* member count, understating every floor the project has read by ~1.9x; no recorded verdict changes under the correction. `paired_folds`' p-value was unreachable, replaced by a blocked test with member nested in arm. Influence: **one row reverses the recall @1% FPR contrast**. |
| [`p2-1-power-analysis-2026-09-14.md`](p2-1-power-analysis-2026-09-14.md) | P2.1 · the power analysis and the gating metric | 2026-09-14 | measured | Recall @1% FPR is demoted from gating (accepted) and pAUC FPR<=0.1 is rejected as its replacement: ROC-AUC detects the same degradation at 1.9x pAUC's z, so the gate reads ROC-AUC with pAUC as a one-sided veto. MDE 0.0131 on TESS at 5 members. Two findings outrank the choice — seed sd exceeds sampling sd for every metric (ROC-AUC 0.0093 against 0.0021), so a paired bootstrap alone under-states the gate's uncertainty; and no metric detects a change confined to the top 5-20% of the ranking, which is the region the product exists to order. |
| [`p2-injection-blocked-2026-09-19.md`](p2-injection-blocked-2026-09-19.md) | P2 · the injection instrument read as a blocked design | 2026-09-19 | measured | Re-analysis of the 990 rows already on disk; nothing injected or trained. **The served champion has no measurable response to an injected transit below S/N 10, and none below S/N 15 on planet hosts** — within-host lift over each host's own S/N=0 cell, t on 39 df. That is #78's thesis with a test attached. #97's GLM treated 990 clustered rows as independent: cluster-robust SEs inflate 1.65x, so z = 13.4 on 875 df becomes **t = 8.1 on 39**, and the effective n is ~322 of 880. Its S/N50 = 19.4 came from a logistic with no floor term, against a measured null-injection floor of 0.264; the half-maximum-lift point is **14.1 overall, 12.7 on FP hosts, 16.8 on planet hosts**. The finding survives, the precision does not, and #97's ~0.009 MDE becomes ~0.011. |
| [`p2-champion-rebaseline-2026-09-19.md`](p2-champion-rebaseline-2026-09-19.md) | P2 · champion re-baseline at M=5 with K2 · pre-registration | 2026-09-19 | pre-registered | Written before the run. The champion carries no variance block, so `decision_floor` borrows `POOLED_SEED_SD` over `n_inc = 1` and the gate's real dual-view MDE is **0.0199, not 0.0126** — the published figure assumes 5 members on both sides. Re-scoring cannot fix it: `seed_sd` is written only by the trainers. Retrains the champion configuration at M=5 on the current shard set (Kepler 2,480 / TESS 2,371 / K2 527), expecting the MDE to fall to ≈0.0125. Falsified if the measured champion `seed_sd` exceeds 0.0093, which would mean every floor read so far was too narrow rather than too wide. Reverses the refusal recorded in `ACKNOWLEDGED_ALARMS`; nothing promoted. |
| [`ml-workflow-audit-2026-09-14.md`](ml-workflow-audit-2026-09-14.md) | end-to-end audit against the taught workflow · #78 part A | 2026-09-14 | audit | Audited against Geron's eight-step checklist, DATA 305 L6, DATA 303 §7.1 and Frery ML II. Largest finding: the fold window is specified in phase and applied in **days**, so the global view spans 1/P of the phase (28.6% at P=3.5 d, 3.3% at P=30) and cannot carry the secondary eclipse the console advertises, while the local view spans 3/P durations rather than 3. Leakage probe built and mutation-tested — 27 straddling hosts when the inner split is ungrouped. Two vacuous test classes found, one of them the audit's own first probe. The seven-step workflow PLAN.md attributes to DATA 301 is in none of the supplied material. |
| [`p2-seed-sd-population-2026-09-20.md`](p2-seed-sd-population-2026-09-20.md) | P2 · the gate's seed sd, on the population it decides on · pre-registration | 2026-09-20 | pre-registered | Written before any code change. `decision_floor` builds the se of a difference from a candidate term measured on **all missions** (`train.py:551`) and a champion prior measured on **TESS**, for a margin the gate reads on the TESS slice alone. Measured on the re-baseline: 0.00225 all-mission against **0.00372** TESS, so every floor this gate has computed understated the candidate's noise by 1.65x. Wiring the re-baseline in as champion unchanged gives a 0.00285 floor where the corrected term supports 0.00471 — too narrow, for a different wrong reason. Adopts the fold-pooled TESS sd, c4-corrected, accepting 4 df to match the estimand; `seed_sd` is left as it is because the record reads it. Falsified if the corrected term is not computable for the runs the gate must decide on, if correcting the candidate's population moves a recorded verdict toward PROMOTE, or if the gate's measured size moves further from nominal. |
| [`p2-gate-size-2026-09-20.md`](p2-gate-size-2026-09-20.md) | P2 · the gate's size under a true null · #115 | 2026-09-20 | measured | **The gate promotes a candidate identical to the champion 28.3% of the time**, and under `strict` 1.1% — and no caller passes `strict`. 10,000 draws over `evaluate_promotion`, both arms the same procedure at the measured TESS per-member sds. The fall-through is geometric: the floor is `2 x se`, the UNRESOLVED band is `[1.33 se, 3 se]`, so a positive margin below `floor/1.5` is outside the band and reaches PROMOTE. REJECT near 50% is deliberate and is not the finding. **Falsifies condition 3 of the seed sd pre-registration**: at 5v5 the corrected term moves false-PROMOTE 23.4% → 29.8%, further from nominal, because widening the floor widens the fall-through. `blocked_contrast` was stubbed — 5,000 permutations per call, 99.6% of the runtime, and it cannot change a verdict. |
| [`p2-unresolved-band-2026-09-20.md`](p2-unresolved-band-2026-09-20.md) | P2 · the UNRESOLVED band's lower edge · pre-registration | 2026-09-20 | pre-registered | Written before any code change. `unresolved_against` marks UNRESOLVED on `[floor/1.5, floor*1.5]`, so a positive margin below `floor/1.5` is outside the band, unrejected, and reaches PROMOTE. Analytic consequences: the rule is **not monotone** — a true margin of 2 se promotes 38.8% of the time against 49.5% at 1 se — and it delivers **49% power at the 2.802 se MDE the record publishes as 80%**, of which 6.8 points come from the hole. Adopts `UNRESOLVED <=> 0 < |margin| <= floor`; the upper edge follows from the MDE formula, since `floor = 2 x se` is the 1.96 critical value and keeping `floor*1.5` would need 3 sigma and give 42% power. Retires `UNRESOLVED_BAND`: floor uncertainty belongs in the floor, not a dead band. Touches recall as well as AUC, in the rejecting direction. Falsified if measured size stays above 5%, if power at one MDE stays below 70%, or if a verdict moves to PROMOTE on a margin that did not clear its floor. |
| [`p2-unresolved-band-result-2026-09-20.md`](p2-unresolved-band-result-2026-09-20.md) | P2 · the UNRESOLVED band, measured · #128 | 2026-09-20 | falsified | The pre-registered change was implemented, measured and **reverted**; `promotion.py` untouched. **Falsified on condition 2**: power at one MDE measured **1.6%** against the 70% floor, and 2.1% at 5 se. Condition 1 did not fire — size fell to 0.1%, *better* than predicted — so **a size-only study would have passed it**. Two findings stand. The shipped band's non-monotonicity is confirmed on the whole gate: PROMOTE is 28.9% at the null, **34.8% at 1 se, 28.5% at 2 se**, and power at the MDE is 35.7% against the published 80%. And the larger one: **98–100% of UNRESOLVED verdicts are raised by the recall guard**, 89% even under the shipped band at 5 se, because `unresolved_against` serves two different questions and the hole beneath the band is wrong for AUC and right for recall. |
| [`p2-recall-one-sided-2026-09-21.md`](p2-recall-one-sided-2026-09-21.md) | P2 · the recall guard objects to good news · pre-registration | 2026-09-21 | pre-registered | Written before any code change. `unresolved_against` tests `abs(margin)` and the recall guard calls it with either sign, so a recall margin between `tolerance/1.5` and `tolerance*1.5` marks UNRESOLVED **whichever way it went** — non-monotone in recall: improve it by half a floor and you promote, by one floor and you are blocked, by two floors and you promote again. At a 5-se AUC margin this blocks **7.8% of all candidates on a recall move that went the right way**, 46% of every UNRESOLVED verdict there. Fix is one line at the call site, `margin < 0 and unresolved_against(...)`; `unresolved_against` itself is untouched so #128 stays separate. **Size will rise, 30.4% to a predicted 33–35%, and that is pre-registered as correct**: the blocking removed was a bug acting as a brake, not a guard. Falsified if any verdict moves REJECT to PROMOTE, if PROMOTE at 5 se does not rise 5 pp, or if any verdict with a negative recall margin changes. |
| [`p2-recall-one-sided-result-2026-09-24.md`](p2-recall-one-sided-result-2026-09-24.md) | P2 · the recall guard fires only on a regression · #131 | 2026-09-24 | mixed | The one-line fix holds on all three falsification conditions: no REJECT moves, no verdict on a negative recall margin moves, and PROMOTE at 5 se rises **71.6% → 79.2%**; the guard now never objects to a recall improvement. **The MDE prediction is falsified** — 40.5% against 42–45% — because 48% of the recall-improved objections there also carried an AUC objection, which recall-first attribution hid. Size rose 30.4% → 33.7%, as pre-registered and accepted. The pre-registration recorded no seed; seed 7 reproduces its baseline exactly and the reading is paired against it. No recorded verdict has a positive recall margin, so none changes. |
| [`p2-auc-floor-rule-2026-09-24.md`](p2-auc-floor-rule-2026-09-24.md) | P2 · the AUC guard promotes only past its floor · #128 · pre-registration | 2026-09-24 | pre-registered | Written before any code change. The AUC call's UNRESOLVED band becomes `(0, floor]` — promote only past the floor — and recall keeps its one-sided band. Chosen from four options costed on the whole gate by substitution (`--options`): at 5 v 5 it takes size **26.7% → 9.0%** on today's floor and to **1.8%** once #123 lands, with 76.6% / 63.7% power at the MDE against an **81.2% ceiling** no AUC rule can pass, because the Brier and ECE tolerances are constants (#134). On real runs, four promote through the hole today at 0.26–0.60x their floors; one, `stage8-synthetic`, moves the other way at 1.009x a floor #123 says is understated, and is UNRESOLVED under `--strict`. Falsified if the implementation differs from the costed rule in any cell, if a verdict promotes on a margin that did not clear its floor, or if a verdict with a margin ≤ 0 changes. |
| [`p2-auc-floor-rule-result-2026-09-24.md`](p2-auc-floor-rule-result-2026-09-24.md) | P2 · the AUC guard promotes only past its floor · #128 | 2026-09-24 | confirmed | None of the three conditions fires and every prediction held exactly: after the change all 48 `--options` rows at seed 7 and at seed 42 are identical to the pre-change `(0, floor]` rows, and across both scans exactly the five predicted verdicts move — four PROMOTEs through the hole become UNRESOLVED, and `stage8-synthetic` promotes at 1.009x its floor. The gate is now monotone, with size **9.0%** at 5 v 5 on today's floor and **1.8%** once #123 lands. One test had asserted the removed upper edge while crediting recall's band for it since 2026-08-17; rewritten to test its own claim. |

## Adding to the record

A new stage gets a new dated file here and one row above. A correction to an
existing file is a dated note under the entry it corrects. Nothing here is
rewritten, renumbered or summarised in place.
