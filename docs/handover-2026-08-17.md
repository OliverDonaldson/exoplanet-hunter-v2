# Handover — finish the gate hardening, then rename Incumbent → Champion

Three of six items from the 2026-08-16 brief are closed. **Two are not started
and one is Ollie's new request.** Nothing was promoted; `ca906040` still serves
and `models/registry.json` is byte-identical (`sha256 0dee467c351ae363`).

Read `docs/roadmap.md` **4.1b** first. It is a *binding pre-registration* written
before any of this was built, and items 1–3 below are read against it.

---

## 1. Non-negotiables

Unchanged. In brief: activate `exoplanet-hunter-v2` first; **never pass
`--promote`, never edit `models/registry.json`**; guards raise rather than
warn-and-continue; one test file per source module; pre-registration is binding —
a result outside it is *falsified*, never re-specified.

**Stop and ask** for: the registry or Fly, a result outside its pre-registration,
re-scoping, deleting non-re-derivable data, `git push` / `dvc push`.

---

## 2. What was closed (`839ff8c`, pre-registered in `9cc8599`)

| # | item | state |
|---|---|---|
| 1 | third verdict, `UNRESOLVED` | **done** |
| 2 | floor = `se` of a difference | **done** |
| 3 | strict alarms, K2 permanence | **NOT STARTED** |
| 4 | dual-view assertions + real-data demo | **NOT STARTED** |
| 5 | missing-labels raises | **done** |
| 6 | `_log1p_centroid` pinned | **done** |

The pre-registered check held: `fc4f3515` read PROMOTE at recall margin −0.0500
against floor 0.0610; it now reads **UNRESOLVED** against 0.0733. All three of
4.1b's predictions confirmed.

**One ordering correction worth knowing.** I implemented the band *after* the
reject threshold and the pre-registered test caught it. Within a criterion the
band is tested **first** — a margin 1.02x its own floor is not a confident
rejection. Only a margin clear of the band on the wrong side rejects. `REJECT`
still dominates across *different* criteria.

---

## 3. What is left

### 3a. Item 3 — strict alarms *(not started)*

`promotion.py`'s `PromotionDecision.alarms` are advisory, and the docstring says
each "owes a written explanation in the roadmap before promotion" — a condition
**no unattended run can satisfy**. Add a `strict` mode that `refresh_pipeline.py`
uses, where an unacknowledged alarm blocks.

**The K2 alarm is permanent and 4.1b already decided how to treat it.**
`incumbent-rebaselined` carries `['Kepler', 'TESS', 'all']` and no K2, so
"populations differ: only the candidate scored K2" fires on every future
candidate. 4.1b records it as **permanently acknowledged**, with the reason, and
explicitly does *not* re-baseline the incumbent — that would move the reference
every past result was read against. Implement an acknowledged-alarm list keyed on
a stable substring, not on the full message.

### 3b. Item 4 — pin the path that actually broke *(not started)*

`test_train.py` calls `_aggregate_cv` but asserts nothing about `per_mission` or
`pooled_gate_recall_seed_sd`. Those assertions exist only in
`test_train_branches.py` — **the trainer that was never broken**. Add them on the
dual-view path.

Then demonstrate on real data. **No dual-view summary on disk carries
`per_mission`**, including `fc4f3515`'s own. Regenerate one from existing
predictions rather than retraining — `evaluate.py summarise` exists for this, and
the mechanism is proven: I did it in-process during this session and the gate
sliced the result. Do not retrain; it is 5 h 28 for nothing.

### 3c. Ollie's request — rename Incumbent → Champion

His words: *"the best model will be known as the Champion model and weekly
refresh challenges the champion."*

A cross-cutting rename, **not** a mechanical find-and-replace:

| surface | note |
|---|---|
| `load_incumbent_summary`, `--incumbent-summary` | public API and a CLI flag; keep the old flag as a deprecated alias or you break every recorded command in the roadmap |
| `models/cv/incumbent-rebaselined/` | a **directory on disk** referenced by `refresh_pipeline.py` and by roadmap prose. Renaming it is a data-layout change |
| `roadmap.md` | "incumbent" appears throughout **inside verbatim pre-registration blocks, which are never rewritten**. Rename forward-facing prose only; leave pre-registrations alone and say so |
| `docs/*.md`, commit history | history is immutable and will keep saying incumbent |

Recommend: rename code and forward-facing docs, keep `incumbent-rebaselined` on
disk with a note, and leave every pre-registration verbatim.

### 3d. A test I did not write

Ollie spotted that the flow had been hard-pinned to `incumbent-rebaselined`,
which would have frozen the comparison forever. **Fixed** — the override is now a
fallback gated on `_incumbent_is_sliceable()`, and disengages permanently once
anything trained since `adf4a71` is promoted.

**There is no test pinning that behaviour.** It is exactly the champion-follows-
promotion property Ollie cares about, and it is currently guaranteed only by
reading the code. Write it: a fake registry pointing at a summary *with*
`per_mission` must produce a command with **no** `--incumbent-summary`; one
pointing at a summary without it must produce the fallback.

---

## 4. Verification

```
python -m pytest pipeline/tests -q -m "not network and not slow"   # 672 passed
python -m pytest api/tests -q                                      # 44 passed
ruff check pipeline api orchestration                              # clean
mypy pipeline/src                                                  # 78 errors
```

**The 78 mypy errors are a pre-existing baseline**, confirmed by stashing and
re-running: 78 before these changes and 78 after. They are missing third-party
stubs, not new. Do not "fix" them as part of this work.

Run the suite **one process per file** for anything touching `train*.py`;
`test_train_branches.py` alone is ~5 min and the suite has known cross-test
contamination in `test_train.py` when run in a single process.

---

## 5. Two things I got wrong this session, recorded so they are not repeated

1. **Stage 2 of the data move missed 14 Path-joined references.** `"data" /
   "labels"` contains no literal `data/labels` for a regex to match. I identified
   this class for stage 1, fixed those by hand, and **failed to re-sweep for
   stage 2** — it reached the API's catalogue default and the entire refresh
   flow. Fixed in `839ff8c`. If any path moves again, sweep both forms.
2. **I hard-pinned the incumbent override**, which Ollie caught. The lesson is
   the same one the vite break taught: a fallback written for one situation
   becomes the permanent behaviour unless something makes it conditional.

---

## 6. State

Branch `v2`, worktree of `/Users/ollie/Project`. Nothing promoted, nothing
pushed. Working tree clean at `839ff8c`.

`fc4f3515ee63452fa6445ab917379ebc` is the calibration run — dual-view, 5 folds ×
3 members on the rolling map, 5 h 28. Its summary still lacks `per_mission`
because it predates `adf4a71`; regenerating it is item 4's demo.
