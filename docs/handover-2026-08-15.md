# Handover — audit the last session, then finish stage 10.5

**Your first job is not to build anything. It is to audit the 2026-08-14/15
session end to end** — how things were tested, implemented, run and written up —
and to report anything that does not reproduce. Only once you are satisfied do
you continue the work in §4.

That session did a lot in one stretch: it closed stage 8, deleted a planning
document and restructured the evidence record, changed **both** trainers, and
produced a headline result for stage 10.5. A single unchallenged session should
not be able to do all of that. Assume nothing in §2 or §3 is right because it is
written down.

---

## 0. Before anything: confirm your objective, then check the gate

**Say back what you are here to do and lay out your plan before touching
anything.** One paragraph of intent, then the checklist, then a plan. Not code.

```bash
cd /Users/ollie/Project/v2 && \
  git log --oneline -7 && \
  git status --porcelain --untracked-files=no && \
  ls models/fold_assignments/stage10_5.json && \
  ls -d models/cv/stage105-{control,propensity} && \
  grep -c "3.11c Result" docs/roadmap.md
```

| check | pass condition |
|---|---|
| the session's commits are present | `e337c1c` at or near HEAD |
| no tracked file is dirty | prints **nothing** |
| the fold artefact exists | one file |
| both branch arms exist | two directories |
| the result is written up | `grep -c` returns **1** |

**Two untracked directories are expected and are NOT yours to commit:**
`data/processed/synthetic_negatives/` and `data/processed/viewset_tfrecords_synneg/`.

---

## 1. The non-negotiables

Unchanged. In brief:

1. **Environment.** `source /opt/anaconda3/etc/profile.d/conda.sh && conda
   activate exoplanet-hunter-v2` before anything.
2. **Never promote.** `ca906040` serves. Do not touch `models/registry.json`.
   **This matters more than usual now** — §3 is the first result that could be
   read as an argument for a serving change. It is not one. That is stage 11
   work and Ollie's call.
3. **Guards raise.** A guard that cannot be observed to fire is not a guard.
4. **One test file per source module.** Scratch work goes outside the repo.
5. **Pre-registration is binding.** A result outside its pre-registration is
   reported as falsified, never re-specified.

**Stop and ask** for: `models/registry.json` or Fly; a result outside its
pre-registration; re-scoping; deleting non-re-derivable data; `git push` /
`dvc push` — commit freely, pushing is Ollie's.

---

## 2. What the last session did — the audit list

Six commits. **Audit each. The claims below are the session's own; your job is to
find the ones that are wrong.**

| commit | what it claims |
|---|---|
| `f535010` | stage 8's prediction 4 measured and written up |
| `2c34e8c` | `plan-2026-08-09.md` deleted and merged into a renumbered `roadmap.md` |
| `8a9f971` | stage 10.5 pre-registration amendment, recorded before the build |
| `9e0c0a5` | the outer split can be pinned from a file, in both trainers |
| `47c4e61` | `n_models_per_fold` on the dual-view trainer, plus the fold artefact |
| `e337c1c` | stage 10.5's recall result |

### 2a. Start here — the reproductions

Each of these should re-derive a number in `roadmap.md` from artefacts. If one
does not, that is the finding.

```bash
# stage 8's four-arm table (3.9b)
python ~/Downloads/.stage8-scratch/measure.py
# prediction 4, the control-arm split and host-AUC (3.9c)
python ~/Downloads/.stage8-scratch/prediction4.py
# stage 10.5's ensemble table (3.11c)
python ~/Downloads/.stage8-scratch/ensemble.py \
  --dualview models/cv/510b565b9f7f444583259d9c8f610e0d \
  --control models/cv/stage105-control --propensity models/cv/stage105-propensity
```

### 2b. Specific claims worth attacking

1. **"All three runs agree on which fold holds every tic, 0 mismatches."** The
   entire joint measurement rests on this. Re-check it directly against
   `models/fold_assignments/stage10_5.json` rather than trusting the reader's
   own assertion, which is the thing under test.
2. **"The n=1 dual-view path is bit-identical to every run before the change."**
   This was *argued* (re-seeding happens only past one member), **not
   demonstrated**. Nobody re-ran a single-member dual-view CV and compared it to
   a pre-change run. If you can falsify it cheaply, do — every earlier dual-view
   number depends on it, including the incumbent's.
3. **The ensemble floor.** `ensemble.py` forms ensemble draw `i` from dual-view
   member `i` and branch member `i`, then takes `2 x sd / sqrt(3)`. Check that
   pairing is the right one and that the stage 6 rule is being applied as stage 6
   states it. The headline is `3.9x` and `4.1x` **of this number**.
4. **`ensemble.py` is scratch and has no tests**, yet it produced the headline.
   Read it as adversarially as you would a source module. One known soft spot: it
   computes `baseline_days` from the *branch* frame and applies it to the
   dual-view scores too. That is valid only because the frames are sorted and
   aligned on `tic_id` — which is asserted in the members-alone loop and **not**
   in the per-arm loop. Confirm the alignment holds, or add the assertion.
5. **The roadmap restructure was verified by a multiset comparison** of
   non-heading lines — it proves no line vanished, **not** that anything landed
   in the right place or kept its meaning in a new context. Spot-check that
   section 3 actually reads chronologically and that no pre-registration block
   was altered. `git show 2c34e8c` and the change log at §7.
6. **The stage-3 finding.** The session claims the incumbent summary needed no
   regeneration and that the "label change invalidates it" note was wrong on both
   counts. Re-derive: does it regenerate byte-identical, and did the 2026-08-08
   refresh really flip zero labels?
7. **W13.** The session claims the `npm audit fix` blocker is gone. Verify
   `frontend/` is clean and the lockfile committed before anyone acts on it.

### 2c. Known soft spots the session would flag against itself

- `n_models_per_fold` on the dual-view trainer is covered by unit tests and a
  **1-epoch** smoke run. No full-length multi-member dual-view run existed before
  the real one, which then produced a headline number.
- The smoke run caught a real bug the unit tests could not (a stale
  `mlflow.log_artifact` on the base checkpoint path, which does not exist once
  members are numbered). **One bug of that shape was found; assume it was not the
  only one.**
- Three-draw floors. `gate_recall_seed_sd` is **0.0677** (E-C) and **0.0935**
  (E-P) against stage 6's 0.0337. The headline margins are 3.9x and 4.1x, so the
  conclusion survives a much wider floor — but check that reasoning rather than
  accept it.
- Timing estimates in this session were wrong twice, in both directions, before
  being measured. Trust only the measured figures in 3.11c.

---

## 3. What stage 10.5 found

Full detail in `roadmap.md` **3.11c**. The headline:

**The branch line's value is as a complement, not a replacement.** Mean-of-logits
recall @1% FPR reaches **0.4362** (E-C) and **0.4223** (E-P) against the
common-fold dual-view member's **0.3046** — **3.9x and 4.1x** their own measured
floors. Both arms clear, so per the amendment the finding is robust to the
weighting and **E-P is carried forward**.

**This reopens nothing about stage 4.** Those five rejections were about
replacement. **Nothing promotes.**

**One falsification worth carrying forward.** E-P's ensemble baseline sensitivity
is **+0.4240, above both its members** (+0.3880, +0.3956). Averaging *created*
baseline dependence rather than inheriting it. Unexplained, and new.

---

## 4. What to do once the audit is clean

### 4a. Finish stage 10.5 — the control-arm pass

`roadmap.md` **4.1**. The one pre-registered measurement still outstanding: the
control-arm split through the stage 7i harness, plus the threshold-free host-AUC
3.11b added beside it.

**It is not one command.** The harness scores a *run directory* and an ensemble
is not one, so it means three passes on the identical draw — the dual-view lane
and both branch lanes — combined per row, with the ensemble's own operating
points derived rather than borrowed. ~1 h build, ~2.5 h compute.

**Confirm the draw reproduces stage 7i's** `580 hosts (290 planet / 290 FP)` and
`1051 routable in both runs` before spending the compute — the fold pinning
changed which rows each model trains on, and if it also changed which hosts are
routable the comparison is across populations rather than models. Simulate it in
pandas first; that costs seconds and `prediction4.py` shows how.

**It tests A3**, the last open prediction: *neither arm moves the control-arm
host-AUC off the ~0.60 stage 8 left it at.*

### 4b. Then, per `roadmap.md` §4

Stage 9 (difference-image branch), stage 10 (Optuna), stage 7ii (attribution),
stage 11 (serving parity), finishing touches, stage 12 (UI).

**One knock-on to raise before stage 11 is planned.** If the complement finding
holds, stage 11 must serve **two** models rather than one, and its 10–15 h
estimate assumes one. Partly mitigated by `ScoringEnsemble.from_registry`
already loading `cnn_dualview.keras`, but it is not costed anywhere.

---

## 5. Open decisions for Ollie — ask, do not assume

1. **The repo cleanup, still undecided.** `data/raw_kepler` is **43 GB** and is
   not in the cleaning audit, which recorded only the 25 GB TESS cache.
   `.mypy_cache` + `api/.mypy_cache` are **167 MB** of pure cache and the only
   zero-risk item; `outputs/` is 46 MB of gitignored Hydra dirs and old logs.
   Nothing was deleted.
2. **W13** — `npm audit fix` is now unblocked. One command, build-time only.
3. **The three-draw floors** — recorded three times now as too thin for the
   decisions they carry.
4. **Whether the qualified second win from stage 8 gets its confirmation.** 4a is
   the cheapest place to fold it in.

---

## 6. Operational facts

- **Nothing long-running goes in `/private/tmp`.** Use `~/Downloads/.stage8-scratch/`.
- **`setsid` does not exist on macOS.** `screen -dmS <name> caffeinate -dimsu
  <script>`. Used for all three CV runs; it works.
- **Do not hold a long Bash waiter against a detached job.** Poll a progress file
  with per-run exit codes, plus a watchdog for the screen session disappearing —
  a success-only grep is silent on a crash.
- **`caffeinate`'s `PreventSystemSleep` only works on AC.** Check it reads 1
  *after* launching.
- **Measured run costs** (2026-08-15, 5 folds x 3 members): dual-view **4 h 44**,
  branch **~2 h 02**. They differ by more than 2x — size them separately.
- **Run the test suite one process per file**; `test_train_branches.py` is ~5 min.
- **Never edit `pipeline/src/**` while a run is in flight.** `docs/` and scratch
  are safe.
- **Both trainers now accept a pinned fold assignment** — `--fold-assignment` on
  the branch trainer, `model.cross_validation.fold_assignment=` on the dual-view
  one. It lands in `run_config`, so runs differing only by folds are
  distinguishable from their summaries. Stages 9 and 7ii face the same
  cross-run comparability problem and can reuse it.

---

## 7. State at handover

Branch `v2`, a worktree of `/Users/ollie/Project`. Nothing promoted;
`models/registry.json` untouched; `ca906040` served since 2026-07-19. Nothing
pushed — pushing is Ollie's.

`results/` and `models/cv/*` are not tracked, so the runs live on disk only:
`models/cv/510b565b9f7f444583259d9c8f610e0d` (dual-view, common folds),
`models/cv/stage105-control`, `models/cv/stage105-propensity`. The fold artefact
**is** tracked at `models/fold_assignments/stage10_5.json`.

Reproduction recipes are outside the repo in `~/Downloads/.stage8-scratch/`:
`measure.py` (stage 8's four arms), `prediction4.py` (the control-arm split),
`ensemble.py` (stage 10.5), plus the `run_*.sh` blocks that produced the runs.
