# Audit — 2026-08-07

A full audit of the model and every recorded metric, triggered by a loss of
confidence in the numbers after the stage 4 *(old 2(a))* resolution run. Read
this before interpreting any result from before this date.

*(Stages were renumbered to consecutive integers on 2026-08-08. This document
uses the new numbers with the old label in parentheses on first mention; the
permanent mapping table is at the top of `roadmap.md`. Run directories on disk
and commit messages still carry the old labels.)*

## The verdict in one line

**The numbers were arithmetically correct. The comparisons were not.**

Every recorded figure regenerated exactly from the artefacts. What was broken
was what those figures were being compared *against*, and the fact that neither
run 1 nor run 2 was a fair test of the branch architecture.

## What survived the audit

- **Every run-1 number reproduces from disk.** Headline per-mission gaps, both
  quartile ladders and their trend slopes, all ten absolute transit bands with
  their row counts, all five span-by-count cells, the K2 benchmark, the noise
  floor. Regenerate with `pipeline/scripts/evaluate.py compare`.
- **The metric implementations are exact.** `SliceMetrics` checked against
  sklearn and against a brute-force sweep of every achievable threshold: zero
  difference on ROC-AUC, PR-AUC, Brier, ECE and recall at 1/5/10% FPR. Pinned by
  `test_slice_metrics_agree_with_reference_implementations`.
- **The leakage guarantee holds structurally.** Both trainers use
  `StratifiedGroupKFold` grouped on `tic_id`.
- **The resolution falsification stands, and is stronger than reported.** Run 2
  lost *every fold* against run 1 (deltas −0.042, −0.049, −0.018, −0.037,
  −0.010; mean −0.0313, d = −1.92). Both runs carried the same defects, so the
  branch-versus-branch comparison is unaffected by everything below.

Two corrections to the record: a Kepler Q2 quartile gap misprinted as +0.0336
(actual +0.0335), and a false claim that "every low-count TESS cell sits at or
below zero" — the narrow-span low-count cell is **+0.0243 on n=417**, the
largest of the three. Neither changes a decision.

## Why the incumbent comparison was invalid

| | incumbent `ca906040` | branch runs |
|---|---|---|
| rows scored | 4,818 | 5,426 |
| K2 rows | 0 | 527 |
| `per_mission` block | absent | present |
| dated | 2026-07-19 | 2026-08-05 / 08-07 |

1. **The incumbent predates the entire stage 2 *(old 1)* rebuild.** Promoted 19 Jul; the
   DV scalars, Gaia RUWE and the merge-collision fix landed 1–5 Aug.
2. **The TESS gate never engaged.** `_gate_slice` returns `None` for a summary
   without a `per_mission` block, and the incumbent has none — so every stage 4
   decision silently fell through to comparing pooled means over different
   populations.
3. **K2 — 9.7% of training — had never been in a comparison at all.**

**Re-baselined** (`results/incumbent_rebaselined.parquet`): the incumbent scored
on the current view set, out-of-fold where it trained and zero-shot where it did
not, never pooled across the two. TESS is 100% out-of-fold on both sides, so the
gating slice is exact.

| slice | n | incumbent | run 2 | gap |
|---|---:|---:|---:|---:|
| K2 *(first measurement)* | 527 | 0.9348 | 0.8741 | +0.0607 |
| Kepler | 2,481 | 0.9915 | 0.9230 | +0.0685 |
| **TESS** *(gates)* | 2,367 | 0.9100 | 0.8944 | **+0.0156** |
| all | 5,375 | 0.9523 | 0.9016 | +0.0507 |

Coverage rose from 4,605 rows across two missions to 5,375 across three, and the
incumbent's own numbers barely moved — so this **validates** the earlier reading
rather than overturning it.

The rescoring path was validated first: reproduced out-of-fold scores match the
stored ones to 2.8e-05 (float32 inference noise), AUC identical to six decimals.

## Why neither run was a fair test of the architecture

Four defects, all fixed, all present in run 1 and run 2:

1. **Missingness was a near-perfect mission indicator.** `NaN → 0.0` at shard
   write, then normalised against constants fitted on present-only rows, put
   "unmeasured" at a real percentile of its own column — a missing
   `odd_even_statistic` at z = −0.679 against a real 5th percentile of −0.68.
   Every Kepler and K2 row is missing; only 12.8% of TESS is.
2. **Two fusion inputs were constants on 56% of rows.** The `detection` and
   `ghost` towers are fed entirely from the DV report and were ungated, so they
   emitted `relu(bias)` for 100% of Kepler and K2 — exactly the "learned bias on
   zeros" the design exists to prevent.
3. **`bootstrap_significance` was a dead input.** Median 3.8e-146, MAD 1.1e-138
   → the degenerate-scale guard substituted 1.0 → the ±10 clip collapsed 1,378
   distinct values to effectively `{−1, 0}`.
4. **Every batch held one mission.** The catalogue is mission-blocked and the
   shuffle buffer was 1,024 over a ~3,470-row split, so `BatchNormalization`
   statistics were computed per-mission and each epoch walked the missions in
   order as an unintended curriculum.

**None of this is resolution.** It is a better explanation for both runs than the
hypothesis that was falsified.

## The recurring defect class

Five instances before this audit, eight found during it: **a check that returns a
plausible answer instead of failing.** Every fix below converts one into a loud
failure.

| finding | was | now |
|---|---|---|
| Missing DV scalars | filled with 0.0 at write | NaN through to the reader, imputed to the fitted centre (z=0) |
| Scalar-only branches | ungated | gated on `dv_usable` |
| `bootstrap_significance` | linear, scale forced to 1.0 | log10 **at write time**, before the float32 cast |
| DV `-1.0` sentinel | unscrubbed on 2 fields | scrubbed; non-positive on a log column is unmeasured |
| A dropped feature column | `log.warning` | `raise` |
| `secondary_phase` | written, normalised, read by nothing | scoped to `secondary_view`; invariant test ties the literals |
| Prediction alignment | asserted on binary labels | asserted on `tic_id` |
| `promote()` | accepted a run with no checkpoints | `assert_servable` refuses it |
| `quarantine_tics` | called by nothing outside its test | persisted by the gate, applied in `run_cv` |
| Zero-shot contamination filter | skipped silently if the file was absent | raises unless explicitly overridden |
| Uninitialised scores | `np.empty` returned as probabilities | `np.nan` + hard raise |
| Unmatched-population gate | compared pooled means | refuses, naming the fix |

### The float32 trap, found by checking the fix

The log transform was first applied at *read* time. That looked right and was
not: **1,639 of `bootstrap_significance`'s 2,235 measured values are below
float32's smallest normal (1.18e-38) and underflow to exactly 0.0 in the
TFRecord**, while `fit_scalar_constants` reads the float64 index. The constants
were fitted on values the model would never see. A read-time transform cannot
recover information the storage format already destroyed. It now happens at
write time, and the shard metadata records `log_scaled_columns`.

Verified after the reshard: distinct z-values 2 → **1,331**.

## Performance

- **`_unfolded`: 122.79 ms → 2.58 ms (48× measured)**, byte-identical output.
  One pass over the cadences instead of one per epoch.
- **Shard decodes per CV run: 20 → 1.** `parse_viewset_shards` caches the decoded
  stream once; only per-fold normalisation reruns. Also four live caches per
  fold → one for the whole run.
- Corrected a docstring claiming ~100 MB for the cached set; it is 670 MB since
  the 2001-bin restore, which was the basis for calling the cache affordable.

## Adopted from ExoMiner

- **Shared conv tower over the flux family.** `local_view`, `odd_view`,
  `even_view` and `secondary_view` are the same measurement at 201 bins and now
  pass through one `TimeDistributed` tower; fusion takes **`odd − even`**, gated
  on both halves. Four independent towers meant an odd-versus-even difference
  was partly a difference between two sets of kernels. **233,617 → 215,281
  parameters**, which also puts the model *below* the incumbent's 227,641 and
  removes the capacity confound that muddied run 2.
- **Paired-fold comparison** (`validation.promotion.paired_folds`). Pairing on
  fold index removes fold difficulty, the larger variance source. Reported and
  alarmed, never blocking: at five folds the two-sided Wilcoxon floors at
  p=0.0625 and can never reach 0.05, so `MIN_PAIRS_FOR_P_VALUE` suppresses the
  p-value below six pairs rather than inviting "not significant" to be read as
  "no effect".
- **N models per fold** (`CVConfig.n_models_per_fold`), averaged before
  calibration, with each member's own AUC recorded. `summary.variance` reports
  `seed_sd` and `fold_sd` **separately** — the decomposition the ±0.0106 has
  always conflated. `seed_sd` is `None` at one model per fold, because one draw
  cannot measure it.

### On ExoMiner itself

Their vetting app is a 270-line static table browser with **no plots**; v2's
console does strictly more per candidate. They have **no calibration at all**
(their own tooltip says the score "is NOT a probability"), **no promotion
concept**, `tf.random.set_seed` appears **zero** times, 258 hardcoded personal
absolute paths, and one genuinely runnable test file. Their ~30 C/C++ files are
inherited Google AstroNet code, duplicated byte-identically in two directories
and wired to nothing.

What they have that v2 does not is **distribution**: a published survey-scale
catalogue with per-row uncertainty, a DOI and a citation ask. That is a
publishing gap, not an engineering gap.

## Still open

*(Status updated 2026-08-08. Items marked **closed** were fixed that night; the
rest stand.)*

- Per-feature normalisation policy as declared config (partially done via
  `LOG_SCALED_COLUMNS`).
- ~~`git_sha` and the resolved model config in `run_config`.~~ **Closed** —
  `utils/provenance.py`; `run_config` now carries `git_sha`, `git_branch`,
  `git_dirty` and the resolved `model_config`. `git_dirty` is deliberately
  three-valued: `None` means git was unreachable, which is a different claim
  from a clean tree and is the one the gate alarms on.
- `score_std` persisted and surfaced in the candidate catalogue; provenance
  headers in `results/*.csv`; precision@k.
- ~~The grouped-CV guard is currently degenerate…~~ **Closed** — `_cache_path`
  now keys on a digest of `(period, t0, duration)`, so two planets on one host
  cannot collide. This **invalidates the existing interim cache**: the next
  build re-derives it from the light curves. `conftest`'s `make_view_set` grew a
  `hosts=` parameter and `test_a_multi_planet_host_never_spans_two_folds`
  asserts what the old count-based test only named.
- `HANDOVER.md` at 2,062 lines has outgrown its format.

### Found and closed on 2026-08-08, from the code audit

- **Calibration never checked convergence or class balance.** `fit_platt` and
  `fit_temperature` returned `res.x` without reading `res.success`, and neither
  guarded a single-class validation split — where the NLL is minimised at the
  extremes, so the optimiser converges and returns a scaler mapping every score
  to one end. That bundle is written to disk as the *servable* calibrator. Both
  now raise.
- **`_gate_slice` fell through to pooled means** when `per_mission` existed but
  lacked TESS, while reporting that the summary "predates the per_mission
  block". `_population_mismatch` compares mission *sets*, so two summaries both
  missing TESS agreed with each other and never triggered the refusal. Now
  raises.
- **A null mission became the string `"nan"`** — `pd.Series.get` returns the
  stored NaN rather than the default, so it keyed its own view cache and
  resolved to a TESS light-curve path. Validated once at catalogue load.
- **`--assemble-only` over a partial cache reported a clean build.** Misses are
  counted and reconciled against the recorded skip reasons; an unexplained
  shortfall raises unless `--allow-partial`.
- **Five merges in `eval/comparison.py` had no `validate=`**, so a repeated
  `tic_id` on either side would silently multiply rows and every comparison
  table would be computed over a population that does not exist.
- **The gate compared TESS slices of different sizes** (2,367 vs 2,399) with
  nothing checking it. Now alarmed.

### The unfolded branch did not do what it says — DECIDED AND REBUILT 2026-08-08

**Verified live 2026-08-08, and it is the whole mechanism of the unfolded-flux
step — old 2(b), now part of stage 7.**
`_branch` (`models/cnn_branches.py:144-151`) documented "convolve each transit
with shared weights, then pool across transits, so the branch sees
transit-to-transit variation rather than an average". The code did:

```python
x = layers.Reshape((transits, view.shape[2] * view.shape[3]))(view)   # (20, 603)
x = _conv_tower(x, ...)                                               # conv along TRANSITS
```

So the convolution slides along the **transit** axis while the 201 phase bins ×
3 channels are flattened into 603 **unordered** channels. The within-transit
phase structure — the transit shape itself — is destroyed before the first
convolution, and there is no weight sharing across phase. The branch cannot see
what a single transit looks like; it can only see how flattened transit vectors
vary down the sequence.

This is also the one place the roadmap's "adopt from ExoMiner" is not adopted:
their `build_conv_unfolded_flux` wraps the tower in `TimeDistributed` over the
phase axis, which is exactly what the comment describes.

**What it cost.** That one 603-channel convolution was **48,256 parameters**,
and the branch as a whole **52,192 — 24.2% of the model's 215,281**. A quarter
of the model paid for the layer that destroyed the transit shape.

#### The decision, and what was built

Put to Ollie with the measurements below rather than picked silently, because
it changes the branch's meaning and not just its wiring. Decided 2026-08-08:
**a dedicated per-transit tower under `TimeDistributed`, pooled with masked
mean + max + spread.** `_unfolded_branch` and `MaskedTransitPool` in
`models/cnn_branches.py`.

**Its own tower rather than the shared flux one.** ExoMiner stacks the unfolded
transits into the same `TimeDistributed` call as the folded local family and
the shapes here would allow it — `unfolded_view` is `(20, 201, 3)` against the
family's `(201, 3)`. Rejected because `BatchNormalization` under
`TimeDistributed` computes its statistics over batch × stack entries, so twenty
low-SNR single transits would set the normalisation for the four high-SNR
folded views as well. That moves `local_view`, the odd/even contrast and
`secondary_view` in the same change that fixes this branch — the branches
carrying most of the current performance — and confounds exactly the
attribution stage 7 *(old D)* exists to do.

**Three statistics rather than an average.** A mean across transits is the
folded view again, which is the thing this branch exists to add to. `mean` is
the reference, `max` catches a single anomalous transit, and the masked spread
is the transit-to-transit variation an eclipsing binary or a background blend
shows and a planet does not. Only the spread measures what the docstring
promised.

**Masking is not an option, and this is the measurement that settles it.**
`preprocess.viewset._unfolded` allocates `zeros((20, bins, 3))` and fills only
the epochs that caught a cadence, so an unused slot is all zeros — presence
channel included. Across the 5,426-row training set:

| | filled slots |
|---|---|
| rows carrying at least one padded slot | **30.4%** |
| rows with fewer than 10 of 20 filled | 16.0% |
| TESS *(gates)*, mean occupancy | 0.758, 25th percentile **0.50** |
| K2, mean filled slots by label | **12.4 planet vs 17.2 false positive** |

An unmasked mean divides by twenty regardless, so the branch's output would
scale with occupancy — and occupancy tracks the label. That is the
observation-baseline confound (stage 8, old 3) arriving through the one branch built
to measure transits rather than hosts.

**Two guards, both of which had to be made to fail before they were trusted.**

- `SPREAD_EPSILON` under the square root. A masked variance is *exactly* zero
  whenever one slot is measured — 17 training rows — and `sqrt` has an infinite
  derivative there, so without it the fold dies on a NaN gradient rather than
  the batch. `test_a_single_measured_transit_gives_finite_gradients` asserts
  both that the default is finite **and that `epsilon=0.0` is not**; a constant
  nobody can observe to matter is one nobody can safely remove.
- `SpreadMeasurable`, a flag on `filled >= 2`. Zero spread from one transit and
  zero spread from twenty identical ones are the same float with opposite
  meanings, and the head cannot recover the distinction from the scoped
  scalars because `observed_transit_count` is uncapped while the stack is
  capped at `MAX_TRANSITS`. Left alone this is the defect class this audit is
  named for.

Every pooled statistic stays finite on an empty stack. The branch is gated
downstream and a gate *multiplies*: `NaN * 0` is `NaN`, so a pool that returned
NaN on the five training rows with no measured transit would poison fusion for
every row rather than contributing nothing.

**Model: 215,281 → 169,361 parameters (−21.3%),** the branch itself 52,192 →
6,272. Stated as a comparability caveat, not banked as a win — see the roadmap's
"training-path changes that break comparability going forward". The capacity arm
tested +19% and moved nothing (paired d = −0.44), so −21.3% is unlikely to be
decisive on its own, but it has not been measured.

**Still sequenced before stage 7's attribution run**, as originally specified:
measuring the old implementation would have answered a question about a branch
nobody intended to build.

### Newly found, still open

- ~~**The inner validation split is unstratified**, in two places…~~ **Closed**
  2026-08-08 — `training/splits.py`. Both affected sites now call
  `stratified_inner_split`; `train.py:132` is untouched, being the holdout it
  always was. The helper is shared rather than the fix applied twice, it returns
  positions into `y` instead of offsets into `trainval` (one frame of reference
  fewer to get wrong), and it **raises** if either side comes out single-class,
  since stratification cannot rescue a population that only has one. The
  regression test pins a real case — 20 hosts, 3 positive, seed 1 — where
  `GroupShuffleSplit` hands the Platt fit a single-class validation split and
  the stratified one does not.

  Timed deliberately: it changes the inner partition and therefore the numbers,
  and it landed alongside the unfolded-branch rebuild, which already forces a
  fresh baseline. One re-baseline covers both rather than two.

  The original finding, for the record — there is a third `GroupShuffleSplit`
  that is *not* this bug, and naming the file is what stops the wrong one being
  fixed:

  | site | what it splits | affected |
  |---|---|---|
  | `training/train_branches.py:376` | inner train/val, feeds the Platt fit | **yes — produces every stage 4 number** |
  | `training/train.py:222` | inner train/val in the incumbent trainer's CV path | yes, same defect |
  | `training/train.py:132` | group-aware train/test *holdout* | **no** — a test split, nothing calibrates on it |

  `GroupShuffleSplit` does not guarantee both classes reach the Platt fit, which
  calibration requires. Safe in production (~868 validation rows) and it is why
  the tiny test fixtures need `val_frac=0.5`. `StratifiedGroupKFold` is the fix;
  `train_branches.py:376` is the minimum, since it is the one behind every
  stage 4 decision. Applying it **changes the inner partition and therefore the
  numbers**, so it belongs between experiments, not between a run and its own
  control.
- ~~**`eval/scoring.py`'s `score_run` is still untested.**~~ **Closed**
  2026-08-08. Eleven tests in `test_eval_scoring.py`, each aimed at a guard that
  stands between a plausible number and a wrong one: `legacy_aux` rebuilding
  index 7 from the catalogue rather than slicing the 13-dim lane (and leaving
  NaN where the catalogue has none, as on all 527 K2 rows); the protocol /
  `fold_of` coupling refused in both directions; a row whose fold has no
  checkpoint raising instead of returning uninitialised memory as a probability;
  zero-shot refusing a run whose trained rows are unknown, and dropping the rows
  it did train on; and `read_views` refusing a stream that does not match the
  index. Fold routing is checked by giving each fold's model its own constant
  output, so which fold scored a row is observable in the score.
- **An eager `GradientTape` over the whole branch model aborts the process.**
  Found 2026-08-08 while testing the unfolded rebuild, then reproduced against
  **HEAD's unmodified model**, so it is the environment and not the change:
  TF 2.17.1 / Keras 3.15.0 on Metal dies inside `_ConcatGradV2` → `split_v` on
  the `fusion` concat. `Fatal Python error: Aborted` — no exception, no
  traceback, and under pytest it takes the whole session with it. The compiled
  path is unaffected: `model.fit` trains normally, which is why no run has ever
  hit this. Anything needing per-variable gradients should probe an isolated
  layer, as `pool_gradients` does, rather than the assembled model.
- **The build's progress line reports a stale cache size.** `build_viewset.py`
  globs `cache/*.npz` while every entry is written to `cache/g{G}l{L}/`, so the
  "cache NNN MB" it logs every 200 targets is the *superseded* 301/31 loose
  cache and is constant for the whole run. Cosmetic — it misreports nothing
  that gates anything — but it is a number in a log that means something other
  than what it says.
